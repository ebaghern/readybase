import React from 'react';
import cx from 'classnames';
import PageHeader from 'components/PageHeader';

const TeamHeader = (props) => (
  <PageHeader className="Team__pageHeader" {...props} />
);

export default TeamHeader;
