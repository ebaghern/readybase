import React from 'react';
import cx from 'classnames';
import PageHeader from 'components/PageHeader';

const ContactHeader = (props) => (
  <PageHeader className="Contact__pageHeader" {...props} />
);

export default ContactHeader;
